{
  "name": "expressive-material",
  "version": "0.0.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "fix": "oxfmt app index.ts --write && oxlint app index.ts --fix && tsc --noEmit",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "oxfmt": "^0.21.0",
    "oxlint": "^1.36.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
