{
  "private": true,
  "type": "module",
  "scripts": {
    "stories": "pnpm --filter stories dev",
    "stories:build": "pnpm --filter stories build",
    "stories:preview": "pnpm --filter stories preview"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.5.3",
      "onFail": "download"
    }
  }
}
